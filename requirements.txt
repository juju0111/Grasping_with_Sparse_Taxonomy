# Core simulation stack — pinned to the versions the bundled checkpoints were
# trained/validated with. See INSTALL.md for the CUDA-specific torch install.
mujoco==3.11.0
warp-lang==1.16.0
mujoco-warp @ git+https://github.com/google-deepmind/mujoco_warp.git@dbc52e3ea69a63e14026e969cb055e0c3c2f0c83
numpy==2.2.6
# torch==2.10.0 (+cu128)  → install from the PyTorch index first (INSTALL.md §3)
hydra-core==1.3.2
omegaconf==2.3.0
PyYAML>=6.0
etils[epath]>=1.14
trimesh==4.11.5
glfw==2.10.0
PyOpenGL>=3.1
termcolor>=2.0
matplotlib>=3.8
opencv-python>=4.9
imageio>=2.35
imageio-ffmpeg>=0.5
wandb>=0.17
